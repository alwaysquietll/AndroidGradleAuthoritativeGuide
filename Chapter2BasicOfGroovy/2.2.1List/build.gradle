task printList {
	doLast{
		def numList = [1,2,3,4,5,6];
		println numList.getClass().name

		println numList[1]
		println numList[-1]
		println numList[-2]
		println numList[1..3]

		numList.each {
			println it
		}
	}
}